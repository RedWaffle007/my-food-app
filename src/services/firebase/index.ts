// Placeholder for the Firebase client (Auth, Firestore, Storage, Functions).
//
// Intentionally empty: no SDK init, no config, no keys in this pass.
// A later step initializes the app here and exports typed handles that the
// feature hooks consume. Remember the hard rule (see CLAUDE.md): FIFO matching,
// escrow, and payout run ONLY in Cloud Functions — the client just calls them.

export {};
